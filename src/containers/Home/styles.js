import { StyleSheet } from "react-native";

export default StyleSheet.create({
    content: {
        margin: "0 auto",
        marginTop: "50px",
        padding: "30px"
    },
    title: {
        textAlign: "center"
    }
});